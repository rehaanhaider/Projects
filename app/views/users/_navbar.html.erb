<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if logged_in?  %>
            <li class="nav-item">
              <%= link_to t('Home'), root_path, class:"nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to t('Profile'), user_path(current_user), class:"nav-link" %> 
            </li>
            <li class="nav-item">
              <%= link_to 'Delete Profile', user_path(current_user), method: 'delete', class: 'nav-link' %>
            </li>
            <li class="nav-item">
                <%= link_to t('Logout'), logout_path, method: 'delete', class:"nav-link" %>
            </li>
          <% else %>
            <li class="nav-item">
                <%= link_to t('Home'), root_path, class:"nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to t('Signup'), new_user_path, class:"nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to t('Login'), login_path, class:"nav-link" %>
            </li>
          <% end %>
      </ul>
      <% if logged_in? %>
        <div class="d-flex">
          <%= form_tag search_path, method: "GET" do %>
            <%= text_field_tag :search, params[:search] %>
            <button class="btn btn-secondary"> <%= t('search_blog') %> </button>
          <% end %>
        </div>
    <% end %>
  </div>
</nav>